# Changelog

All notable changes to the {{SDK_NAME}} SDK will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [1.0.0] - Unreleased

### Added

- Initial release of {{SDK_NAME}} SDK
- TypeScript/JavaScript SDK with full type safety
- React SDK with hooks and context provider
- Python SDK with async support
- Go SDK with idiomatic error handling
- Java SDK with Maven publishing
- Kotlin SDK with coroutines support
- PHP SDK with Composer publishing
- Swift SDK with Swift Package Manager
- Ruby SDK with gem publishing
- Rust SDK with Cargo publishing
- Dart SDK with pub.dev publishing
- .NET SDK with NuGet publishing
- HTTP client with retry (exponential backoff + jitter)
- Circuit breaker pattern (5 failures → open → 30s reset)
- HMAC-SHA256 request signing
- API key rotation support
- PII detection in error messages
- Error sanitization
- 80% test coverage enforcement
