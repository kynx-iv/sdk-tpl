/// The current version of the {{SDK_NAME}} Dart SDK.
const String sdkVersion = '{{SDK_VERSION}}';

/// Returns the full user-agent string for this SDK.
String userAgent() => '{{SDK_SLUG}}-dart/$sdkVersion';
